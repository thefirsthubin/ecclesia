/**
 * Placeholder entry point for the Pastoral Care bounded context's framework-agnostic domain logic (PRD §13.2). Real content - the Follow-up workflow rules and the silent-drift decision tree (PRD §15.8) - lands in the Pastoral Care domain-modeling milestone.
 *
 * This module is scaffolding: it exists so the library registers as a
 * real, buildable, testable Nx project ahead of the milestone that adds
 * its actual contents. It intentionally contains no business logic, no
 * database models, and no authentication code, per Sprint 0 scope.
 */
export const PASTORAL_CARE_LIB = 'pastoral-care' as const;
