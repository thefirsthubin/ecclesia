/**
 * Placeholder root component. Proves the React/webpack/Nx wiring works
 * end to end. Real screens (per-role dashboards, PRD §16.6; configuration
 * surfaces, PRD §16 "Platform & Administration") are added module by
 * module in later milestones - this component intentionally contains no
 * product UI.
 */
export function App() {
  return (
    <div>
      <h1>Ecclesia Admin Console</h1>
      <p>Sprint 0 scaffold - no screens implemented yet.</p>
    </div>
  );
}

export default App;
