/**
 * Placeholder entry point for the Insights bounded context's framework-agnostic domain logic (PRD §13.6). Real content - the Church Pulse weighted-scoring model (PRD §12.8) - lands once apps/worker's Engagement Signal consumer exists.
 *
 * This module is scaffolding: it exists so the library registers as a
 * real, buildable, testable Nx project ahead of the milestone that adds
 * its actual contents. It intentionally contains no business logic, no
 * database models, and no authentication code, per Sprint 0 scope.
 */
export declare const INSIGHTS_LIB: "insights";
//# sourceMappingURL=insights.d.ts.map