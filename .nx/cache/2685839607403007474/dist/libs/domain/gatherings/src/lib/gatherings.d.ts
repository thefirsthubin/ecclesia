/**
 * Placeholder entry point for the Gatherings bounded context's framework-agnostic domain logic (PRD §13.4). Real content - the generalized Gathering type hierarchy (Blueprint §12.4) and recurrence-series handling - lands in the Gatherings domain-modeling milestone.
 *
 * This module is scaffolding: it exists so the library registers as a
 * real, buildable, testable Nx project ahead of the milestone that adds
 * its actual contents. It intentionally contains no business logic, no
 * database models, and no authentication code, per Sprint 0 scope.
 */
export declare const GATHERINGS_LIB: "gatherings";
//# sourceMappingURL=gatherings.d.ts.map