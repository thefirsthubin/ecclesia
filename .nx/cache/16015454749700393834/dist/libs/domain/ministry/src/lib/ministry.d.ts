/**
 * Placeholder entry point for the Ministry bounded context's framework-agnostic domain logic (PRD §13.3). Real content - Basonta staffing-adequacy calculation and overcommitment detection - lands in the Ministry domain-modeling milestone.
 *
 * This module is scaffolding: it exists so the library registers as a
 * real, buildable, testable Nx project ahead of the milestone that adds
 * its actual contents. It intentionally contains no business logic, no
 * database models, and no authentication code, per Sprint 0 scope.
 */
export declare const MINISTRY_LIB: "ministry";
//# sourceMappingURL=ministry.d.ts.map