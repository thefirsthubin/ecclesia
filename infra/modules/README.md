# infra/modules

Reusable CDK constructs (VPC, ECS Fargate service, RDS, ElastiCache,
EventBridge bus) shared across environments (Blueprint §11.3-§11.4).

**Status:** populated in the CDK infrastructure milestone.
